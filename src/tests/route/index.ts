import { Router } from "@virtualstate/navigation/routes";
import { Navigation } from "@virtualstate/navigation";

{

    const router = new Router();

    const { route } = router;

    route("/", ({ request }) => {

    })

    route("/test", ({ request }) => {

    })

    route("/json", ({ request }) => {

    })

}